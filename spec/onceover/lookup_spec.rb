require "spec_helper"

RSpec.describe Onceover::Lookup do
  it "has a version number" do
    expect(Onceover::Lookup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
